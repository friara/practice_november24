import 'package:calendar_ver1/domain/entity/event/event.dart';
import 'package:flutter/material.dart';

class EventViewModel with ChangeNotifier {
  //final EventRepository _repository;
  //final AddEventUseCase _addEventUseCase;

  //EventViewModel(this._repository, this._addEventUseCase);

  // Future<List<Event>> getEventsForDay(DateTime day) async {
  //   //return await _repository.getEventsForDay(day);
  // }

  // Future<void> addEvent(Event event) async {
  //   //await _addEventUseCase.execute(event);
  //   //notifyListeners();
  // }
}